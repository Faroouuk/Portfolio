import { projects } from "@/data/projects"; import ProjectCard from "./ProjectCard"; import Reveal from "./Reveal"; import SectionHeading from "./SectionHeading";
export default function FeaturedProjects(){return <section id="projects" className="section"><div className="shell"><Reveal><SectionHeading label="02 / Selected work" title="Products, not just projects." intro="A closer look at the systems I design, build, and operate."/></Reveal><div className="mt-14 space-y-5"><Reveal><ProjectCard project={projects[0]}/></Reveal><Reveal className="grid gap-5 md:grid-cols-2">{projects.slice(1).map(p=><ProjectCard key={p.name} project={p}/>)}</Reveal></div></div></section>}

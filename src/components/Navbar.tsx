"use client";
import { Code2 as Github, Menu, X } from "lucide-react"; import { useEffect,useState } from "react";
const links=[["Projects","#projects"],["Experience","#experience"],["Skills","#skills"],["About","#about"],["Contact","#contact"]];
export default function Navbar(){const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(window.scrollY>24);f();window.addEventListener("scroll",f,{passive:true});return()=>window.removeEventListener("scroll",f)},[]);return <header className={`fixed inset-x-0 top-0 z-50 transition-all ${scrolled?"border-b border-white/10 bg-[#090a0c]/90 backdrop-blur-xl":"bg-transparent"}`}><nav className="shell flex h-18 items-center justify-between" aria-label="Main navigation"><a href="#top" className="font-mono text-sm font-semibold">AF<span className="text-lime-400">.</span></a><div className="hidden items-center gap-7 md:flex">{links.map(([l,h])=><a key={h} href={h} className="text-sm text-zinc-400 transition hover:text-white">{l}</a>)}<a href="https://github.com/" target="_blank" rel="noreferrer" aria-label="GitHub (placeholder)" className="text-zinc-300 hover:text-white"><Github size={18}/></a></div><button className="md:hidden" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></nav>{open&&<div className="border-t border-white/10 bg-[#090a0c] px-5 py-5 md:hidden">{links.map(([l,h])=><a key={h} href={h} onClick={()=>setOpen(false)} className="block border-b border-white/5 py-3 text-zinc-300">{l}</a>)}</div>}</header>}
